# FinMind API token —— fetch_stock_list.py 用它抓股票清單（TaiwanStockInfo）。
# 沒設的話會送出空 token：免費層仍可能通，但會被更嚴格地限流，而且失敗時是
# 靜默回空清單 —— 而 fetch_stock_list 是 make bootstrap / make update 的第一步，
# 清單空掉整條流程等於白跑。請務必設定。
#
# 用法：cp .env.example .env 然後填入自己的 token
# 申請：https://finmindtrade.com/
FINMIND_TOKEN=your_finmind_token_here
