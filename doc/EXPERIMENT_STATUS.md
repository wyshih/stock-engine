# 實驗狀態交接（更新 2026-09-02）

跨 session 的交接文件。對話會斷、agent 會被 session limit 砍掉，這份不會。
**開工前先讀這份**，再讀 `doc/BACKTEST_LOG.md`。

---

## 現在的狀態（2026-09-02）

| 項目 | 狀態 |
|---|---|
| 資料層 | ✅ 2019-01-02 ~ 2026-08-21 全部重建 |
| 兩個模型 | ✅ 各自調參 + 訓練（P1 重建後的版本） |
| 門檻曲線 | ✅ 兩條，dedup=False 口徑 |
| **門檻** | ✅ **使用者 2026-08-26 挑定**（見 `bundle.py` 的 `CHOSEN_THRESHOLDS`） |
| **回測** | ✅ **20 列已跑完**，記錄在 `doc/BACKTEST_LOG.md` #29 |
| public 資料包 | ⬜ 尚未產出（`make export-public`） |

### 2026-09-02：收到 2 個模型（使用者要求）

現行模型：`m1_base_up20` 與 `m1_mdd10`。兩者同一份 base 特徵集、同一個搜尋空間，
**只差標的** —— 差異只能來自標的，不會混進調參的運氣。
（編號留空號是刻意的，`m1` 對得上 BACKTEST_LOG 的 ①。）

移除的四個與它們**專屬**的上游：

| 移除 | 連帶移除 | 理由 |
|---|---|---|
| `m2_nomkt_up20` | — | 去大盤變體；訊號數對齊後墊底（見下） |
| `m3_v3_up20`、`m8_v3_nobear` | v3 特徵管線（`engine/features/v3/`、`features_v3.parquet`、`feature_audit.csv`、`volproxy.csv`、`make features-v3`） | 只有這兩個在用 |
| `m6_base_nobear`、`m8_v3_nobear` | `label_nobear`（`build_labels_nobear.py`、`labels_nobear.parquet`、`score_source.py` 的去空頭過濾） | 只有這兩個在用 |

`m1_mdd10` 同時被補進 `train_all.sh` —— 它原本是 2026-08-28 手動跑
`train_label_variant` 產的，不在任何重建路徑裡，違反最高原則。

挑定的門檻與測試期表現（2026-08-26 那輪回測，BACKTEST_LOG #29；
被移除的四個留在表裡當紀錄）：

| 模型 | 門檻 | 測試期筆數 | 勝率 | 平均報酬 | Sharpe | 現況 |
|---|---|---|---|---|---|---|
| m1_base_up20 | 0.77 | 14,867 | 84.5% | +16.71% | 0.674 | **現行** |
| m1_mdd10 | 0.68 | — | — | — | — | **現行**（2026-08-28 加入） |
| m2_nomkt_up20 | 0.74 | 9,222 | 87.0% | +17.55% | 0.668 | 已移除 |
| m3_v3_up20 | 0.70 | 18,883 | 82.1% | +15.43% | 0.658 | 已移除 |
| m6_base_nobear | 0.62 | 13,012 | 73.2% | +12.45% | 0.629 | 已移除 |
| m8_v3_nobear | 0.60 | 14,164 | 72.2% | +11.83% | 0.613 | 已移除 |

⚠️ **但訊號數對齊後 m2 墊底**（Sharpe 0.423 vs 其他四個 0.727~0.750）——
絕對門檻的高報酬來自門檻效應，不是模型能力。詳見 BACKTEST_LOG #29。

### 2026-09-02：新增 label_steady20（實驗中，未畢業）

現行 `label_up20` 偏袒大跌反彈，實測證據與五個試過無效的方向都寫在
`engine/models/build_labels_steady.py` 的模組 docstring（那是唯一來源，不要在這裡
複製一份會漂移的副本）。摘要：

| 候選 | 崩跌/持平偏差 | 正例中緩漲% | 正例報酬中位 |
|---|---|---|---|
| `label_up20`（現行） | 1.78 | 53.3% | +5.2% |
| 站上 5 日線 ≥ 10 天 | 1.41 | 56.6% | +4.5% |
| 站上 20 日線 ≥ 10 天 | 1.05 | 60.0% | +4.2% |
| **`label_steady20`** | **0.98** | **63.3%** | **+18.6%** |

（全市場基準：緩漲 56.7%。「崩跌/持平」= 進場前跌>20% 的正例率 ÷ 持平的正例率，
越接近 1.0 越沒有偏差。）

**狀態（2026-09-03 更新）**：已訓練並回測完畢 —— **偏差修好了，但績效輸，不出貨**。
完整數字見 `doc/BACKTEST_LOG.md` #31。摘要：訊號中崩跌股 18.9%→7.3%、
緩漲 32.8%→85.2%（label 側成功），但訊號數對齊回測每一項都輸 m1
（Sharpe 0.445 vs 0.777、20 日持有勝率 44.9% 低於擲硬幣）。

病根不是調參：正例率隨市場環境擺盪 5.3%~14.5%（`label_up20` 各期穩定 35~39%），
val_sel AUC 只有 0.5541。

### 2026-09-03：第二輪 label_xsrank20（橫斷面排名）——同樣不出貨，見 BACKTEST_LOG #32

改成「當日全市場風險調整後報酬排名前 20%」，正例率確實釘死（跨切分擺盪 1.00 倍，
實測 0.200/0.200/0.200/0.200/0.200），分數上限也摸到了滑桿下限（0.5010）。
**但績效比 label_steady20 更差**：val_sel AUC 0.5478、無上限持有勝率 50.1%
（等於擲硬幣）、Sharpe 0.264，是三個 label 裡最弱的。

**最貴的教訓**：label 側全歷史分析算出的偏差是 1.12（幾乎打平），但模型真正選出
的高分訊號（每日前 1.5%）崩跌股佔比仍有 18.1%（`label_up20` 是 18.9%）——
**幾乎沒改善**。label 的全歷史統計不能代表模型高分訊號的組成，兩者是不同的
群體。往後驗證新 label 方向，必須先訓練、看真實高分訊號的組成，不能只看
label 本身的統計就下結論。

`m1_steady20` 與 `m1_xsrank20` 都留在 `EXPERIMENTAL_KEYS`，bundle 與分數檔留著
供對照。⚠️ 兩輪都**沒有調參**（借 m1 的組態，`config_source_key` 有留痕）。

**建議暫停「修 label_up20 崩跌偏差」這個方向** —— 兩輪都顯示「數天數」的設計
雖有偏差，卻是唯一讓模型學出穩定訊號的定義；改法目前都讓可學習性明顯下降。

<details><summary>原本的執行步驟（已完成，留作參考）</summary>

```bash
make labels     # 產 data/labels_steady20.parquet
make train      # m1_steady20 會跟著調參 + 訓練 + 產曲線
make curve      # 若曲線已存在會跳過
# ← 這裡由人看 data/sigcurve_m1_steady20_val_sel.csv 挑門檻
# 挑好之後三件事一起做：EXPERIMENTAL_KEYS 移出 → CHOSEN_THRESHOLDS → MODEL_KEYS
make backtest   # 規則 9：必須看訊號數對齊那張表才算數
```
</details>

⚠️ 基準率從 39.7% 掉到 10.0%，**AUC 與現行兩個模型不可直接比較**，學習難度也變高。
label 側的分析只證明「目標不再偏袒崩跌反彈」，**模型學不學得起來、訊號賺不賺錢
要看回測**。

### 怎麼跑

```bash
make bootstrap      # 從零抓資料（依主機分流，約 3~4 小時）
make rebuild-full   # 清衍生檔 → 特徵 380 欄 → label
make train          # 兩個模型各自調參 + 訓練
make curve          # 產門檻曲線 → 由人看曲線挑門檻
make backtest       # 絕對門檻 + 每日前 1.5% 訊號對齊，兩張表
make verify-vs-old  # 對照舊 repo 逐項驗證
```

⚠️ **`make rebuild-full` 之後若要真的重訓，得先 `make clean-models`** —— `train_all.sh`
的跳過判斷只看「bundle 檔存在」，否則會留下用舊特徵調出來的組態與舊模型。

### 已不存在的東西（舊文件會提到，別去找）

`run_queue.sh`、`finalists.py`、`train_bundles.py`、`train_submodels.py` 都是委員會
時代的腳本，**沒有搬進本 repo**（見 CLAUDE.md 的「不要搬回來的東西」）。
`best_config()` 是唯一從 `finalists.py` 抽出來的部分，現在住在
`engine/models/sweep_config.py`。

---

## 調參的現行做法（2026-08-23 使用者訂的兩條規定）

**一、每個模型各自調參，不得共用組態。** 每個模型用自己的特徵集、自己的 label
跑一輪搜尋，讀自己那份 `data/sweep_{key}_rf.csv`。即使特徵集相同、只差 label
也各搜各的。`search_space()` 對沒定義空間的 key 直接 raise，不會 fallback。

**二、搜尋與訓練用同一個樹數 300，不分兩階段。**

現行搜尋空間（`sweep_round1.py` 的 `MODEL_SPACES`，使用者逐項確認）：

| 模型 | max_features | max_depth | min_samples_leaf（固定） |
|---|---|---|---|
| m1 / m2 / m6 | 15, 20 | 10, 20 | 200 |
| m3 / m8 | 15, 20 | 15, 30 | 400 |

每模型 4 組、合計 20 組。錨點取自 norf 封存 bundle 的實際參數（base 家族
20/200/15、v3 家族 20/400/30）。`n_jobs=8`、`class_weight=None`。

⚠️ `min_samples_leaf` 移出搜尋空間是因為實測它既沒訊號也不影響速度
（AUC 全距 0.0013、耗時比 1.02x），留著只是讓組數翻倍。

**調參只評估 `val_sel`** —— 不碰測試期（那等於把測試期攤在眼前），也不需要
`val_es`（RF 沒有 early stopping）。

---

## Round 4 切分（2026-08-14 定案，已寫進 `train_single.py`）

```
train    2020-01-01 ~ 2023-11-30   1,738,484 列  正例 40.4%
（embargo 2023-12）
val_es   2024-01-01 ~ 2024-06-30     223,942 列  正例 38.4%   調參目標
val_sel  2024-07-01 ~ 2024-12-31     241,573 列  正例 31.8%   挑門檻
（embargo 2025-01）
test     2025-02-01 ~ 2025-12-31     444,374 列  正例 35.0%
test2    2026-01-01 ~ 2026-07-31     251,189 列  正例 35.2%
```

相對 Round 2 的改善：訓練資料 +35%、挑門檻樣本 ×2。
**已知缺點**：`val_sel` 基準率 31.8% 比測試期低 3pp（2024 上下半年差異大），
在偏難的期間挑門檻、套到偏易的期間，訊號會比預期多 —— 方向上偏保守。

Round 1/2/3 完全沒動，舊實驗仍可重現。

## 【歷史】Round 4 搜尋空間（2026-08-15）

⚠️ **以下為歷史記錄，不是現行做法。** 現行是每模型各自調參、每模型 4 組，
見上方「調參的現行做法」。這段留著是因為它記錄了當初為什麼選那個範圍。

```
max_features      10, 15, 20      舊範圍下邊界是 20，往更小找
min_samples_leaf  200, 400, 500
max_depth         15, 30, 45
n_jobs            6（10 核機器）
```

**搜尋與訓練用同一個樹數 300，不分兩階段**（2026-08-23 使用者指定）。

舊做法是「第一階段固定 150 棵掃組態、第二階段取前幾名再試 300/450」，理由是樹數
主要影響變異不影響偏誤、150 棵的組態排名與 300 幾乎一致。問題有兩個：

1. **第二階段從來沒跑過** —— 正式模型一直是 150 棵（從封存 bundle 讀出來確認：
   `n_estimators=150`）。也就是把篩選階段的暫定值當成最終組態，而那個值當初只是
   為了加速。
2. 「排名會轉移」是**假設不是事實**。搜尋與訓練用同一個樹數就不需要這個假設。

代價是搜尋慢一倍，換掉的是一個沒被驗證的前提。

封存：`data/sweep_round4_rf_stage0_300trees.csv` 是舊空間、300 棵樹跑的 2 組，
與第一階段不可比，僅供參考。

---

## Label 實驗

現行正式 label 只有 `label_up20`（未來 20 日上漲天數 ≥ 10，基準率 0.375）。

| Label | 定義 | 基準率 | 狀態 |
|---|---|---|---|
| `label_up20` | 未來20日上漲天數 ≥ 10 | 0.375 | **現行使用中**（m1_base_up20） |
| `label_mdd10` | 同上，且未來20日最低收盤不跌破 −10%（跌破的正例改標 0） | 0.361 | **現行使用中**（m1_mdd10） |
| `label_nobear` | 同上，但當下空頭排列的列整列排除（不標 0） | 0.394 | ❌ 2026-09-02 隨 m6/m8 移除 |
| `exit_return > 7%` | 套現行出場規則的實際報酬 | 0.510 | ✅ 已訓練，已上前端 |
| `label_A` | 未來20日最大漲幅 > 1.5σ | 0.239 | ⬜ 已算好，未訓練 |
| `label_B` | 相對大盤 20日超額 > 3% | 0.280 | ⬜ 已算好，未訓練 |
| `exit_return > 1.0σ` | 上者的波動標準化版 | — | ⬜ 未開始 |

中間產物（scratchpad，**最貴、不要重算**）：
- `newlabel/label_exit.parquet` — 300 萬列全歷史 `exit_return`，已與 `simulate()`
  逐筆對帳（最大差 1e-7）
- `labels_ab/labels_ab.parquet` — 含 `sigma20` / `mfe20` / `excess20` / `label_A` / `label_B`

### 7% label 的結果與兩個紅旗

2024 年（現行系統最痛的一年）：新 label 模型 **+3.5~4.4%**，現行 r2_rf@0.47 是 **−1.58%**，
同期全買 −0.98%。方向正確。

**但結論還不能定案**：
1. **平均持有 127 個交易日**（中位 78 天）—— 遠超一個月 embargo。訓練期末端樣本的
   答案落在驗證期裡，val 段的 AUC 與門檻曲線都被污染。只有 test(2024) 距訓練
   結束 13 個月相對乾淨。**要繼續得把 embargo 拉到 6~12 個月重跑。**
2. **右設限偏誤逐年惡化**：未定比例 2020 年 0.5% → 2024 年 4.7% → 2025 年 14.4%
   → 2026H1 **47.5%**。活下來的都是「走得快的」，會系統性推高近期基準率。

---

## ⚠️（已成歷史）v3 三組模型無法逐欄重現（2026-08-22，搬進 engine repo 時發現）

> 2026-09-02：v3 家族（m3/m8）與整條 v3 特徵管線已移除，這一節不再有作用點。
> 保留是因為它記的是**方法論教訓**（資料相依的中間產物會讓模型無法逐欄重現），
> 日後任何依賴 `feature_audit.csv` 這類產物的設計都會再踩一次。

**結論：m3 / m8 重建後拿到的特徵數是 518，不是封存 bundle 的 509。
base 與 nomkt 的 m1 / m2 / m6（344 / 332）不受影響。**

（2026-08-22 模型從十個縮到五個，v3nomkt / v3nomv 那四個已砍，但下表保留
完整對照，之後若想復原仍然適用。）

| 特徵集 | 封存 bundle | 用現行官方資料重建 | 差 |
|---|---|---|---|
| base | 344 | 344 | — |
| nomkt | 332 | 332 | — |
| v3 | 509 | 518 | +9 |
| v3nomkt | 497 | 506 | +9 |
| v3nomv | 481 | 490 | +9 |

**原因不是程式被改壞。** `features/v3/spec.py` 與 `transforms.py` 與舊 repo
byte-identical（已 `cmp` 驗過）。真正的原因是 v3 的「sz / raw 變體要選哪一個」
依賴 `data/feature_audit.csv`，而那份稽核檔是**資料相依**的 —— 它用當時的全市場
資料算 Spearman 相關來分類特徵。換成 TWSE/TPEx 官方資料源之後，有 8 個特徵的
分類翻轉，另外多出 `bb_upper_break_log_xs` 等 `_log_xs` 欄。

**這代表什麼**：v3 那兩個模型只能重新訓練出「同一套方法、新資料下的版本」，
不能宣稱是封存 bundle 的重現。拿新舊 v3 模型的績效直接對比是**不合法的比較** ——
特徵集不同、訓練資料也不同，兩個變因混在一起。

**怎麼確認自己踩到這件事**：`make verify-vs-old` 會印出上面那張對照表。
2026-09-02 起它只檢查 base 一組（v3 特徵檔已不存在），base 漂移就是真的有問題，要查。

## 2026-08-23 從零重建：三個漏列的相依與抓取節流的教訓

第一次真正「從乾淨環境重建」，抓出舊 repo 一直沒被發現的問題。

### 舊 repo 的 requirements.txt 漏列三個套件

| 套件 | 誰要用 | 為什麼一直沒被發現 |
|---|---|---|
| `optuna` | `sweep_round1.py` | 只裝在當時那個 venv 裡。乾淨環境會在 `make train` 的調參階段才炸 |
| `plotly` | `frontend/technical_chart.py` | 同上。技術面分析頁一開就死 |
| `lxml` | `fetch_revenue.py` 的 `pandas.read_html` | 是 pandas 的 **optional dependency**，缺了不會在 import 時報錯，要跑到那一行才丟 ImportError —— 連 AST 相依掃描都掃不出來 |

三個都已補進 `requirements.txt` 並註明原因。

### 抓取的併發規則：依主機，不依資料源

把 bootstrap 按「資料源」切成五組併行，其中四組打的都是 `www.twse.com.tw`，
等效間隔被壓到 1 秒以下：

- 單執行緒 3 秒間隔：8 小時 33 次瞬時失敗，全部自癒
- 五組併行：**15 分鐘 48 次失敗**，出現 502 與 HTML 錯誤頁，最後被 WAF 擋成
  307（回 784 bytes 的「因為安全性考量，您所執行的頁面無法呈現」）

封鎖的形狀值得記：**只有最近三天的日期還回 200，其餘全 307** —— 近期日期在
邊緣快取拿得到，需要回源的歷史日期一律被擋。停止所有請求後約 1 分鐘解除。

同日的官方 API 調查也實測到：以 2~3 秒間隔打 `twse.com.tw` 約 6 個請求就會被擋；
**TPEx 側則完全沒有節流**。

結論寫進 Makefile 的兩條鐵則：
1. 同一台主機只能有一條連線
2. 寫同一個 parquet 的不能同時跑

### 行情回補慢一倍，是因為沿用了 `--market both`

`fetch_price_official.py` 預設 `--market both` 會在**同一個行程裡**先打 TWSE、
sleep 3 秒、再打 TPEx、sleep 3 秒 —— 每天 8 秒。但那是兩台互不相干的主機。

舊 repo 的做法（`logs/backfill_twse.log` 與 `backfill_tpex.log` 第一行時間戳同一秒
可以證實）是兩個行程並行、各自寫獨立檔案，再合併：

```
--market twse --out price_official_twse   1h55m（3.0s/天）
--market tpex --out price_official_tpex   2h27m（4.0s/天）
合併驗算：1,844,981 + 1,496,899 + TWII 1,854 = 3,343,734 ✔
```

並行後 2h27m，序列則要 4h22m。本次第一輪用了預設值，白花約 2 小時。
現行 Makefile 已改成分開抓，合併步驟補成 `merge_price_sources.py`
（舊 repo 這步是手動做的、沒留下程式，跟 `promote_price` 當初的情況一樣）。

### 官方 API 調查結論：沒有更省的抓法

TWSE OpenAPI 130+ 個端點、TPEx 225 個，**沒有一個帶日期參數**，全是當期快照；
`data.gov.tw` 只是 OpenAPI 的目錄殼，沒有歷史包；唯一整段下載在付費 E-Shop 且
是 tick 檔。唯一的「多天」端點是個股×月（`STOCK_DAY` / `tradingStock`），
換過去要 144,000 個請求 vs 現在的 3,988，**貴 36 倍**（損益平衡點約 25 檔）。

**逐日抓 1,994 天已經是請求數最少的做法**，慢是端點形狀決定的，不是實作問題。

附帶發現：上櫃的本益比／殖利率／淨值比有官方來源
（`tpex.org.tw/www/zh-tw/afterTrading/peQryDate`，涵蓋 2007 起，實測回 812 檔），
可以補掉 `fundamental` 目前上櫃整欄 NaN 的缺口，代價是 +1,994 個請求。尚未實作。

⚠️ 另一個坑：舊 TPEx 路徑 `web/stock/aftertrading/…` **會忽略日期參數、無條件回
最新交易日，而且回 200 不是錯誤**。任何還在用舊路徑做歷史回補的程式，會把今天的
快照重複寫進每一個日期。本專案已改用 `www/zh-tw/afterTrading/otc`，不受影響。

## ⚠️ 舊 repo `stock_committee_norf` 的 data/ 有 31 天的資料瑕疵（2026-08-24 查出）

從零重建後用 `make verify-vs-old` 逐項比對，發現的**不是新資料有問題，是舊資料
有問題**。這件事直接影響怎麼解讀 `doc/BACKTEST_LOG.md` 裡的舊記錄。

| 檔案 | 壞掉的範圍 | 判定依據 |
|---|---|---|
| `fundamental` | **25 天** × 全市場（`per` / `pbr` / 殖利率） | 抽查 40 筆，40 筆（100%）是舊值脫離自身趨勢 |
| `chip` | **6 天** × 約 1,000 檔（三大法人、融資融券） | 抽查 40 筆，16~22 筆是舊值與該股其他日期重複 |

chip 的六天：2022-08-22、2022-08-23、2024-06-11、2024-10-01、2025-02-26、2025-09-22。

### 決定性證據

**fundamental**（個股 1101 的 `per`）：

```
2021-12-30   新 48.00   舊 48.00
2022-01-03   新 47.45   舊 34.10   ← 只有這天差 39%
2022-01-04   新 47.30   舊 47.30
2022-01-05   新 47.15   舊 47.15
```

前後日完全一致，只有那一天脫離。而 `34.1` 這個值在舊資料裡還出現在 2022-05-12、
2023-08-14、2024-01-17 —— 同一個錯誤值重複出現在多個日期。

**chip**（個股 1101 的 `foreign_buy`）：舊 repo 在 2025-09-22 給的是 2,051,000，
而**完全相同的數值也出現在 2024-06-11** —— 那正是另一個差異日。同一筆錯誤資料
寫進兩個不同日期，是「抓到錯誤回應卻照寫」的典型特徵。新資料在該日的值
（39,891,074）是唯一的，前後五個交易日新舊完全一致。

### 影響

- 6,314 列 chip 差異中，**5,761 列真的會進特徵與模型**（不是只有 ETF）
- 級聯放大：`fundamental_features` 的 `per_rank` 有 **90 萬列**不同 —— 排名是
  橫斷面的，一天的值錯了，那天全市場的排名就全錯
- `relative_features` 的滾動百分位同理，`rel_chipz_*` 有 29~75 萬列不同

**結論：舊 repo 上跑出來的實驗結果，都帶著這 31 天的污染。** 這不會讓 BACKTEST_LOG
的定性結論作廢（污染是零星的、不是系統性偏向某個方向），但任何要求「逐列可重現」
的比較都不該拿舊資料當基準。

### 比對工具因此新增了歸責能力

`engine/tools/verify_vs_old.py` 現在不只報「不符」，還會判定**是哪一邊有問題**：

- `blame_by_trend`：值在時間上應該連續的欄位（本益比、股價）→ 看誰脫離鄰近日水準
- `blame_by_duplicate`：每日流量型欄位（三大法人買賣超）跳動劇烈、趨勢法判不出來
  （實測新舊各 9 筆平手）→ 改看誰的值與該股其他日期重複

判定看**單邊**的不對稱，不看佔比 —— 「兩邊都重複」多半是 1000、2000 這種整數的
巧合，不具鑑別力。

## ⚠️ 月營收：MOPS 的歷史頁會被重新產生，已下市公司補不回來（2026-08-25）

從零重建時發現 **169 檔在市股票完全沒有月營收**，查出兩個獨立原因。

### 原因一：只抓了「國內公司」那一份（已修，補回 123 檔）

MOPS 的整月彙總頁把同一個月拆成兩個檔案：

```
https://mopsov.twse.com.tw/nas/t21/{sii|otc}/t21sc03_<民國年>_<月>_<0|1>.html
                                                                    ↑
                                            _0 = 國內公司　_1 = 國外公司
```

`_1` 是第一上市／上櫃的 **-KY 公司與 -DR 存託憑證**。舊版 `fetch_revenue.py` 只抓
`_0`，於是 120 檔 -KY 加上 9105 泰金寶-DR 完全沒有營收資料。實測 2019-01~2026-07
兩個市場的 `_1` 檔都存在（`_2` 是 404）。現在 `MOPS_BULK_SUFFIXES = ("0", "1")`，
一個月從 2 個請求變 4 個。

### 原因二：MOPS 用「現行名單」重產歷史頁 —— 這個補不回來

剩下的 46 檔補不到，因為 **MOPS 會用當下的公開發行公司名單重新產生歷史彙總頁**，
已下市／合併消滅的公司會從歷史檔案中被移除。實測：`2888 新光金` 在 2021-03 的
檔案裡已經不存在，同業 `2891 中信金` 則在。

這 46 檔**全部已停止交易**（最新的一檔最後價格是 2022-04），所以對模型沒有實際
影響 —— 它們有價格資料的那段期間，舊 repo 的營收也是靠當時逐日抓下來的存量。

**但這是「從零重建」的一個真實限制，要記著**：

- 已下市公司的歷史月營收**無法從 MOPS 重建**，只能靠當時抓下來的存量
- 也就是說 `revenue.parquet` 對舊資料而言是**唯讀的歷史資產**，重建只補得到現存公司
- 若日後真的需要那 46 檔，唯一來源是舊 repo `stock_committee_norf/data/revenue.parquet`

同性質的風險也適用於 `stock_list.parquet`：它是「現在的名單」，不含已下市公司，
所以 `promote_price.py` 才刻意用「篩掉 `^00` 的 ETF」而不是「篩 stock_list」——
後者會把 26 檔已下市的真個股一起濾掉（2026-08-22 驗證過）。

## 已知的方法論陷阱（踩過的坑）

- **固定百分比門檻會製造波動度偏誤**：P(未來20日最大漲幅>7%) 從最低波動組 20.9%
  到最高波動組 66.0%，Spearman +0.316。除以 σ 後降到 −0.13。
- **分數不可跨 label 比較**，AUC 也不可 —— 基準率與問題難度都不同。最後只有
  同一套回測下的實際報酬能比。
- **門檻每次重訓都要重挑**，絕對值搬不動。方法固定：驗證期預測機率由高到低，
  **每一個機率值都當一次門檻**跑一遍，畫曲線由人看圖挑，不用「取前 N%」或任何
  自動規則。回測一律呼叫 `backtest.py` 的 `simulate()`/`performance()`，`dedup=False`。
- **不要並行訓練**：10 核機器，RF 內層已吃 8 核。三件同時跑 load average 衝到 23，
  互相搶資源反而每件都變慢（2026-08-14 實測）。

---

## 前端

模型選單改為**掃描 `models/` 目錄自動列出** —— 實驗模型只要把 bundle 丟進
`models/bundle_<key>.pkl` 就會出現（標 🧪），不用改程式。
配套檔案：`data/score_<key>_<split>.parquet`、`data/sigcurve_<key>_val_sel.csv`。

個股歷史預測頁可**複選模型**，把多條分數線疊在同一張圖上比「誰在漲之前先亮」。

啟動：`make app`（或 `make go` 連同資料更新）。
